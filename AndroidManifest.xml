<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.kyu"
    android:versionCode="1"
    android:versionName="1.0" >

    <application
        android:debuggable="true"
        android:icon="@drawable/utils"
        android:label="@string/app_name" >
        <activity
            android:name=".ImUtils"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FormActivity"
            android:label="FormActivity" />
        <activity
            android:name=".WakeOnLanListActivity"
            android:label="WakeOnLanListActivity" />
        <activity
            android:name=".WeatherActivity"
            android:label="WeatherActivity" />
        <activity
            android:name=".InformationTheme"
            android:label="information"
            android:theme="@android:style/Theme.Dialog" />
        <activity
            android:name=".PopupTheme"
            android:label="popup"
            android:theme="@android:style/Theme.Dialog" />
    </application>

    <uses-sdk android:minSdkVersion="7" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />

</manifest>