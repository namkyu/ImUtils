<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="kr.kyu"
      android:versionCode="1"
      android:versionName="1.0">
    <application android:icon="@drawable/utils"
    			 android:label="@string/app_name"
    			 android:debuggable="true">
        <activity android:name=".ImUtils"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

		<activity android:name=".AddActivity" android:label="AddActivity" />
		<activity android:name=".ScheduleActivity" android:label="ScheduleActivity" />
		<activity android:name=".WakeOnLanListActivity" android:label="WakeOnLanListActivity" />
		<activity android:name=".WeatherActivity" android:label="WeatherActivity" />
		<activity android:name=".DinnerActivity" android:label="DinnerActivity" />
		<activity android:name=".InformationTheme" android:theme="@android:style/Theme.Dialog" android:label="information" />
		<activity android:name=".PopupTheme" android:theme="@android:style/Theme.Dialog" android:label="popup" />
    </application>

    <uses-sdk android:minSdkVersion="7" />
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
</manifest>